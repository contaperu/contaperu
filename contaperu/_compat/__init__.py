"""Las rutas de la 0.x, con sus firmas de la 0.10, mientras dure la 1.x.

`contaperu/operaciones.py`, `generar.py`, `cli.py` y `servidor_mcp.py` quedan como módulos que solo resuelven nombres,
con aviso (`_obsoleto.reexportar`); lo que resuelven vive aquí o en su sitio nuevo. Nada del motor importa este
paquete (`tests/test_capas.py`): se retira entero en la 2.0.
"""
